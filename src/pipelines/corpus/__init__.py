"""
Corpus-related pipelines (link discovery, PDF fetching, etc.).
Importable as `pipelines.corpus.fetch_pdfs` and runnable via `python -m pipelines.corpus.fetch_pdfs`.
"""

__all__ = ["discover_links", "fetch_pdfs"]
